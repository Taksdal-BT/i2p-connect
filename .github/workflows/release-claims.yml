name: Release Claims

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  claims:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run release claim guard
        shell: pwsh
        run: .\scripts\check-release-claims.ps1
