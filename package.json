{
  "name": "i2p-connect",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first I2P Connect status domain logic.",
  "type": "commonjs",
  "main": "dist/src/status/statusMapper.js",
  "types": "dist/src/status/statusMapper.d.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc --outDir dist",
    "test": "tsc --outDir dist && node dist/tests/status/statusMapper.test.js"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
